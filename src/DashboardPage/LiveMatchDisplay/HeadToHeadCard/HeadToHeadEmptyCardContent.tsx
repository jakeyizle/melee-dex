import { CardContent, Box } from "@mui/material";

export const HeadToHeadEmptyCardContent = () => {
  return (
    <CardContent>
      <Box sx={{ display: "flex", justifyContent: "center" }}>
        No Match History
      </Box>
    </CardContent>
  );
};
